DONE -> Erledigt
REM  -> Remember/Merke
Vn	 -> Bsp. V2 geplant für Version 2.0

allgemein
(alle)		- Konzept für eine Seite
		- Integration von Stats/Docu-Flaws ins Admin-Tool
		- Tutorial zu Axis und .Net-WSDL.exe

Admin-Tool (TODOs aus Martins Email)
		- Menüpunkt „Web Service erstellen“ ist noch nicht implementiert.
		  evtl. die Standard-Einstellungen aus Settings beachten
		- es sollte so sein, dass die Quelltext-Doku und die UserDoku in der DB sind. 
		  Wenn eine Methode neu aufgenommen wird soll die Doku aus dem Source in beide Felder. 
		  Dann kann der Admin die UserDoku so lassen oder bearbeiten (z.B. auch komplett leer machen). 
		  Die Quelltext Doku sollte in die DB damit der Admin informiert wird, dass die Doku im Source geändert wurde.  
	DONE    - Menüpunkt „Web Service registrieren“. Hier muss nur noch das letztendliche registrieren gemacht werden.
		- beim Wizard muss wie erstens noch generiert werden, evtl. bei Settings noch den Pfad für das generierte Zeug abfragen.
		- DOKUMENTIEREN:
			Funktionsweise, Config-File, PHPBINDir
    DONE- access new WSDL Generator class constants from smarty templates
	V2	- Fehler in der Quellcode Doku anzeigen
    V2  - UDDI registration of the generated Web Services
        
        - WARNUNG ausgeben, dass scannen des quellcodes mit gefahren verbunden ist?
    V2  - Ausgabe der Doku Fehler, in optisch ansprechnder Art und Weise

WSDL Generator
DONE - test cases for the generator (http://qa.php.net/write-test.php)
DONE - finalization method
DONE - filter methods by policy plugin
DONE - wsdl:documentation before every method if given by policy plugin
DONE - constants as class constants
     - add documentation when adding functions [should be triggered by an optional argument of addFunction()]
     - verify doc-lit and rpc-lit output with WS-I Basic Profile 1.1
       Possible Flaws:
       R2206 rpc-lit
V2   - XML Schema in external .xsd-file
V2   - XML Schema validation against http://schemas.xmlsoap.org/wsdl/soap/2003-02-11.xsd
REM    (a better validation engine is needed. The DOM extension has problems with the xsd)
V2   - generator tool eventuell auch als cmd-line tool anbieten?!
V2   - XML Beautifier
V3   - redesign (driver architecture)
V3   - WSDL 2.0 support


webservices identifizieren
(martin?)	  - sowohl scan anbieten als auch explizites auswählen von dateien auf dem server
				welche webservices funktionalität bereit stellen
				explizites auswählen von den entsprechenden php dateien und ihren abhängigkeiten wird
				eventuell notwendig, da es keinen standard gibt, wie die scripte zu gestallten sind
				manche führen beim includen schon funktionalität aus, was gefährlich sein könnte
				(z.B. deleteDatabase.php als admin script)


libs/reflection
(stefan)	      - weitere tags hinzufügen
		 DONE - tool zum prüfen der doku auf bestimmte aspekte
(Falko)               - improve and fix XML Schema output
		 REM  - unterstützt werden nur einfache arrays, bei den der index nicht relevant ist,
		        Klassen und einfach datentypen, KEINE Maps (Key=>Value) Typen
		        
REST
		DONE  - ser/deserialisieren der eingabedaten?
		      - objekt collections handhabung?
		DONE  - angabe von url schemata zur identifizierung von steuerparametern
		V2    - wie könnte man wsdl dazu gestallten? -> WSDL2.0
			für Toolkit Version 2.0 vielleicht ein Generator denkbar
			  
		nice to have für REST Server:
		DONE  - beim erstellen wählbar, ob mit oder ohne authentfizierung
		DONE  - wahl des providers, angabe der Klasse, da eh alle Klassen geladen wurden, können wir die passende Datei
		DONE    über die reflection api ermitteln
		DONE  - XSLT transformation angebbar über f=<zielformat> z.B. f=xbel für die bookmarks
		DONE  - @restin und @restout für spezielle serializer
		DONE  - standardserializer im generator angebbar
		DONE  - weitere serializer für den standardparameter ?&f=<serializerkürzel>
		DONE  - im generator als allgemeines und im @restin/@restout
		DONE  - reihenfolge: angabe über &f -> @restin Angabe -> globale angabe, default im dd
		DONE  - wobei angabe &f sich nur auf ausgabe bezieht
			
		V2	  - angabe des status-codes über @ muss möglich sein, um hier sinnvolle rückgaben ermöglichen zu können
		V2	  - weitere sinnvolle header senden?
			  - known issues auflisten (erkennen der pattern abhängig von reihenfolge im quelltext)
			  - abhängigkeiten von pear packages auflisten
		V2	  - unterstützen  der HEAD operation und der OPTIONS operation um fähigkeiten/möglichkeiten der ressourcen auslesen zu können
			  - html oberfläche für REST Generator
			  - stats.php robuster gestallten
			  - PHPDoc Tag Parser überarbeiten, mehrzeilige kommentare an @tags ermöglichen
		V2	  - REFLECTION API: mechanismus hinzufügen, der bei fehlenden kommentaren, die elternklassen absucht um eventuell 
			    vorhandene kommentare zu übernehemen
			  - mit obrigem feature auch noch einen globalen klassenobjekte cache einrichten, um die performance zu steigern.
		V2	  - anhand der position des @param tags auf den dazugehörenigen parameternamen schließen, wenn nicht angegeben
		
		
!!!!!!!!	  - Anpassen der Pfade beim Generieren und kopieren der Server